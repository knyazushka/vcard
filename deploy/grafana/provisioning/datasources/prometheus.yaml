# Источник данных заводится из файла, а не руками в интерфейсе:
# пересозданная Grafana поднимается уже готовой.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
